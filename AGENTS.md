# Agents

- Always use `uv run` to execute agents and scripts.
- Always use Abseil (absl) libraries for flags and testing: use `absl.flags` instead of `argparse`; use `absl.testing` / `absltest` for tests.
- Ask me to run sudo commands for you; you can't do it yourself.
